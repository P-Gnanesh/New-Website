This Repository is mainly about the init commands used to create a new git repo from the terminal..

That is, we can create a git folder using init command or convert an existing local project into a git folder which is tracked by git
[Syntax: git init]